---
name: knowledge-query
description: Retrieve a compact project-curated framework or migration fact when API or platform behavior is uncertain.
---

# Knowledge Query

Query the narrowest relevant channel and term. Use retrieved knowledge to design a falsification test or implementation constraint; do not dump entire manuals into context.

Channels: `wordpress`, `vue`, `tailwind`, `svg`, `seo`, `web-platform`.

If the answer is version-sensitive and the curated entry is insufficient, mark it as unresolved and consult authoritative external documentation rather than guessing.
